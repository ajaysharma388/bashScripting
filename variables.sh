#! /bin/bash
# we have two types of variable in the shell scripting 
# a. System variables (gernally are in capital letter's).
# b. User defined varibles (gernally are in lowercase).
echo $BASH
echo "This $BASH simply prints the path for the bash shell."

